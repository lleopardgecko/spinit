# spinit (data)

This public repo serves only `ingredients.js` via GitHub Pages at
https://lleopardgecko.github.io/spinit/ingredients.js — the live Spin It tool
fetches it from there.

Source code lives in a separate private repo.
